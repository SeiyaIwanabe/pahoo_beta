<% if current_user != post.user %>
  <% if post.user.senderd_by?(current_user) %>
    <%= link_to user_relationships_path(post.user.id), class: "sendingLink", method: :delete, remote: true do%>
      <i class="fas fa-check-circle"></i>
    <% end %>
  <% else %>
    <%= link_to user_relationships_path(post.user.id), class: "sendingLink", method: :post, remote: true  do%>
      <i class="fas fa-plus-circle"></i>
    <% end %>
  <% end %>
<% end %>
